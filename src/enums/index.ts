export enum FormErrorsTypes {
  Request = 'request',
  Email = 'email',
  Password = 'password',
}

export enum FormErrors {
  Email = 'Sorry, email is not valid',
  Password = `Sorry, password length have to be more that 5 char`,
}